{
  "name": "flowagent-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Per-user stdio-to-streamable-HTTP bridge for the Power Automate (FlowAgent) MCP server",
  "main": "bridge.mjs",
  "scripts": {
    "start": "node bridge.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
